databases:
  - name: neuralgrid-db
    databaseName: neuralgrid
    user: neuralgrid
    plan: free

services:
  # ── Backend (FastAPI via Docker) ───────────────────────────────────────────
  - type: web
    name: neuralgrid-backend
    plan: free
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: neuralgrid-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://neuralgrid-frontend.onrender.com
      - key: STATIC_DIR
        value: ""
      - key: PYTHONUNBUFFERED
        value: "1"

  # ── Frontend (Vite → static site) ─────────────────────────────────────────
  - type: web
    name: neuralgrid-frontend
    plan: free
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://neuralgrid-backend.onrender.com
