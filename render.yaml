services:
  # ── Backend (FastAPI via Docker) ───────────────────────────────────────────
  - type: web
    name: neuralgrid-lite-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false          # Set manually in Render dashboard — paste your existing DB connection string
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://neuralgrid-lite-frontend.onrender.com
      - key: STATIC_DIR
        value: ""
      - key: PYTHONUNBUFFERED
        value: "1"

  # ── Frontend (Vite → static site, always free) ────────────────────────────
  - type: web
    name: neuralgrid-lite-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://neuralgrid-lite-backend.onrender.com
